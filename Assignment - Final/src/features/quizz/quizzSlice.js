import { createSlice } from "@reduxjs/toolkit"


const initialState = {
       
}

const quizzSlice = createSlice({
    name : 'quizz',
    initialState,
    reducers : {

    }
})

export default quizzSlice.reducer ;
export const{} = quizzSlice.actions ; 